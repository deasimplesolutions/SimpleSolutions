{
  "name": "simple-solutions-chat-proxy",
  "version": "1.0.0",
  "description": "Backend proxy for Simple Solutions chat widget — keeps the Anthropic API key private.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.0"
  },
  "engines": {
    "node": ">=18"
  }
}
